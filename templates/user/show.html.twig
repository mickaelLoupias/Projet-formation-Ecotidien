{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ user.name }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user.password }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ user.age }}</td>
            </tr>
            <tr>
                <th>Sexe</th>
                <td>{{ user.sexe }}</td>
            </tr>
            <tr>
                <th>Composition</th>
                <td>{{ user.composition }}</td>
            </tr>
            <tr>
                <th>Level</th>
                <td>{{ user.level }}</td>
            </tr>
            <tr>
                <th>Progression</th>
                <td>{{ user.progression }}</td>
            </tr>
            <tr>
                <th>Recompense</th>
                <td>{{ user.recompense }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ user.location }}</td>
            </tr>
            <tr>
                <th>Logement</th>
                <td>{{ user.logement }}</td>
            </tr>
            <tr>
                <th>Subscribedate</th>
                <td>{{ user.subscribedate ? user.subscribedate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>
   
    <a href="{{ path('user_index') }}">back to list</a>

    <a href="{{ path('user_edit', {'id': user.id}) }}">edit</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
